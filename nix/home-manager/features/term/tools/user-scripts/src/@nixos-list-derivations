#!/usr/bin/env bash

set -euo pipefail

PROFILE="/nix/var/nix/profiles/system"
LIMIT="${1:-10}"

if ! [[ "$LIMIT" =~ ^[0-9]+$ ]] || [ "$LIMIT" -lt 1 ]; then
    echo "Usage: $0 <nombre_de_generations>"
    exit 1
fi

sudo nix-env --list-generations --profile "$PROFILE" |
    tail -n "$LIMIT"
